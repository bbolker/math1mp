## basics

### data types and data structures

- lists, tuples: indexing, slicing, appending, extending
- mutability of lists and dictionaries
- strings: indexing, slicing, setting
- dictionaries: `.keys()` and `.values()` methods, `in`, creation and indexing

### flow control etc.

- `for` and `while` loops, including nested loops
- `if`, `else`, `elif`
- `break`
- `print`

### functions

- arguments, including default arguments
- `return()`
- print vs return vs break

### file reading

- opening
- reading via `.read()` (whole body), `.readlines()` (read lines into list), or `for` loop (read one line at a time)
- `.strip()`
- `.split()`

## numpy

### functions

- `arange`
- zeros
- ones
- reshape

### array methods

- `axis=` argument to methods
- .sum()
- .mean()
- .prod()
- .min()
- .max()
- .argmin()
- .argmax()

### array attributes

- .shape (returns the shape of a numpy array, or a pandas DataFrame, as a tuple)
raising errors

## pandas

- Series and DataFrames
- slicing and indexing (.loc, .iloc, reference columns as attribute, reference columns as lists, reference rows by slicing index)
- .index, .columns, .shape attributes
- referencing by logical expressions
- methods: .sum, .mean
